# Supabase (client) — exposés au browser, donc utiliser uniquement la clé "anon"
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Groq API — NE JAMAIS exposer côté client.
# Utilisée uniquement dans les Edge Functions Supabase (Deno).
GROQ_API_KEY=
